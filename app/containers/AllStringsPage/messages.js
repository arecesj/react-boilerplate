/*
 * AllStringsPage Messages
 *
 * This contains all the text for the AllStringsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllStringsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllStringsPage container!',
  },
});
